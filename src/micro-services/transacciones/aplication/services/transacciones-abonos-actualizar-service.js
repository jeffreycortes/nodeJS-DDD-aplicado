//const mappingToFingerprint = require('../../aplication/mappers/fingerprint-device-mapper');
const TransaccionesRepository = require('../../infrastructure/repositories/transacciones-repository');

const actualizarTransaccionAbono = async (params) => {
  //mappear en entidad
  //mappear fingerPrint
  //consumir a ACH

  return new Promise(resolve => { resolve('Abono rechazado') });
}


module.exports = actualizarTransaccionAbono;
